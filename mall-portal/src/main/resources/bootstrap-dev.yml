spring:
  mail:
    #邮件协议smtp
    host: smtp.qq.com
    #发送者的邮件的用户名
    username: 1280075151@qq.com
    #使用的编码
    default-encoding: utf-8
    #移动端客户授权码(在邮箱中设置)
    password: wrnwqlpletnufhed
  cloud:
    config:
      profile: dev #启用环境名称
      label: master #分支名称
      name: portal #配置文件名称
      discovery:
        enabled: true
        service-id: mall-config
eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: http://localhost:8001/eureka/
  instance:
    hostname: ${eureka.instance.ip-address} #决定服务跳转host，必须指定
    ip-address: localhost #指定ip 可通过外网访问
    prefer-ip-address: true
    instance-id: ${eureka.instance.ip-address}:${spring.application.name}:${server.port}
